import 'package:template/src/bootstrap/bootstrap.dart';

extension AppBootstrapNetwork on AppBootstrap {
  // void listenNetworkStateService(ProviderContainer container) {
  //   container.read(networkStateServiceProvider);
  // }
}
